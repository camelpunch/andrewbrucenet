#!/usr/bin/env bash

set -eu

cwd=$(dirname "$0")

cd public
python -m SimpleHTTPServer&
pid=$!
cd -

$cwd/wkhtmltopdf \
    --print-media-type \
    http://localhost:8000/cv/ public/cv.pdf

kill $pid
